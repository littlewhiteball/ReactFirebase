export default (id, title, description, visibility, start, closing, fulfillment, options) => ({
    id,
    title,
    description,
    visibility,
    start,
    closing,
    fulfillment,
    options,
});
